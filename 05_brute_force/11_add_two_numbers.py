def solution(numbers):
    answer_sums = {numbers[i] + numbers[j] 
                  for i in range(len(numbers))
                  for j in range(i+1,len(numbers))}
    return sorted(answer_sums)

# --------------------------------------------------------------------------
# 풀이 메모 — gist 코멘트에서 옮겨온 기록이다 (2026-08-29 이관).
# 문제를 풀며 남긴 시행착오이므로 당시 문장을 그대로 둔다.
# 원본: https://gist.github.com/silver-ring-data/a717e9e3640c8d01679209b1ed227818
#
# [2026-03-11]
# [문제 설명]
# 정수 배열 numbers가 주어집니다. numbers에서 서로 다른 인덱스에 있는 두 개의 수를 뽑아 더해서 만들 수 있는 모든 수를 배열에 오름차순으로 담아 return 하도록 solution 함수를 완성해주세요.
#
# [제한사항]
# numbers의 길이는 2 이상 100 이하입니다.
# numbers의 모든 수는 0 이상 100 이하입니다.
#
# [2026-03-11]
# ### [init] : 문제는 해결했으나, 일부 최적화 필요
# - 이중 루프의 범위를 최적화하여 중복 연산(A+B, B+A) 제거해야함. (불필요한 연산 제거)
# - range()의 시작점을 조절해 불필요한 if-continue 조건문 제거 권장
#
# [2026-03-11]
# ### [Refactor] : 일부 불필요한 연산 제거, 불필요한 조건문 제거
# - but, Set 생성 방식을 좀 더 효율적으로 변환 가능 : 현재 불필요한 변환이 많이 진행되고 있음
#
# [2026-03-11]
# ### [opt]
# - sorted() 함수의 다형성(Iterable 수용) 및 리스트 반환 특성 이해
# - 불필요한 형변환 함수(list(), set()) 호출을 줄여 가독성 최적화
# - 변수명 수정
# - enumerate -> range
# --------------------------------------------------------------------------
