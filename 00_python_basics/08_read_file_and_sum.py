result = 0
with open('numbers.txt','r') as f:
  for line in f :
    result += int(line.strip())
print(result)
  
  

# --------------------------------------------------------------------------
# 풀이 메모 — gist 코멘트에서 옮겨온 기록이다 (2026-08-29 이관).
# 문제를 풀며 남긴 시행착오이므로 당시 문장을 그대로 둔다.
# 원본: https://gist.github.com/silver-ring-data/d35dd5c25a48009c3badc2f3d3f2468a
#
# [2026-03-05]
# bug : int() 함수는 이 명단 전체를 한꺼번에 숫자로 바꿀 수 없기 때문에 명단에 있는 줄(line)을 하나씩 꺼내서 숫자로 바꾼 뒤 더해주는 과정이 필요
#
# [2026-03-05]
# fix : 공백에 대한 우려 -> strip으로 제거 필요
# --------------------------------------------------------------------------
