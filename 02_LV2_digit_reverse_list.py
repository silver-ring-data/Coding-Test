'''
#https://school.programmers.co.kr/learn/courses/30/lessons/12932

[문제정의] : 자연수 n을 뒤집어 각 자리 숫자를 원소로 가지는 배열 형태로 리턴해주세요. 예를들어 n이 12345이면 [5,4,3,2,1]을 리턴합니다.

[제한 조건]
n은 10,000,000,000이하인 자연수입니다.

[입출력 예]
n	return
12345	[5,4,3,2,1]
'''

def solution(n : int) -> list[int]:
    """자연수 n을 뒤집어 각 자리 숫자를 원소로 가지는 배열 형태로 리턴하는 함수

    1. n을 문자열로 변환
    2. 슬라이싱을 이용해 n의 순서를 바꾸어줌
    3. 바꾼 순서를 배열로 다시 변환
    4. 각 요소들을 다시 int로 변환

    Args:
        n : 입력된 자연수

    Returns:
        answer : 숫자를 원소로 가지는 배열
    """
    return [int(char) for char in str(n)[::-1]]

'''
🧐 왜 이 코드가 더 "클린"할까? (이유 분석)
1. 리스트 컴프리헨션 사용 (Effective Python Better Way 7)
이유: for 루프를 돌며 append를 호출하는 방식보다 리스트 컴프리헨션이 훨씬 간결하고 의도가 명확해. 파이썬 내부적으로도 리스트를 한 번에 생성하기 때문에 append를 여러 번 호출하는 것보다 속도가 더 빠르단다.

2. 불필요한 조건문 제거
이유: 코딩 테스트에서 "제한 조건"은 입력값의 범위를 알려주는 가이드라인이야. 네 코드의 if n > 10000000000처럼 예외 처리를 직접 넣으면, 실제 시험에서는 불필요한 연산이 될 수 있어. 파이썬다운 코드는 불필요한 검사보다는 핵심 로직에 집중하는 거야.

3. 명시적인 타입 힌트
이유: list라고만 쓰는 것보다 list[int]처럼 리스트 안에 무엇이 들어있는지 명시해주면, 나중에 코드를 유지보수할 때 훨씬 편해. (이건 Python 3.9 버전부터 표준이 된 방식이야!)

4. 언더바(_)를 활용한 숫자 표기 (추가 팁)
만약 꼭 숫자를 비교해야 한다면 10000000000 대신 10_000_000_000이라고 써봐. 쉼표처럼 숫자를 끊어 읽기 편하게 해주는 파이썬의 배려야.
'''
